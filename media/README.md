# Add your videos here

Save the two supplied clips in this folder with these names:

- `our-moment-01.mp4`
- `our-moment-02.mp4`

The website detects them automatically and replaces the illustrated memory cards with looping, muted videos. The site works normally if you only add one clip or neither clip.

Open `index.html` in a modern browser after adding them.
